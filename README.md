# dataeng-project

## Setting up environment

1) `docker-compose up airflow-init`
2) `docker-compose up`
3) open `http://localhost:8080`, add postgres connection

## Streaming sub project

Streaming sub project represents separate environment and flow and can be found [here](dataeng_streams)