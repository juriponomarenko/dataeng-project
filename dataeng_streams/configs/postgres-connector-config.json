{
    "name": "postgres-sink",
    "config": {
        "connector.class": "org.ut.cs.dataeng_streams.connector.sink.JDBCPostgresSinkConnector",
        "topics": "joined_stream",
        "connection.url": "jdbc:postgresql://db:5432/dataeng_kym",
        "monitor.thread.timeout": 1000,
        "tasks.max": "1",
        "auto.create": false
    }
}
